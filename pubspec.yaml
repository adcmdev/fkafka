name: fkafka
description: Kafka equivalent in flutter
version: 2.0.2
homepage: https://github.com/adcmdev/fkafka

environment:
  sdk: ">=2.16.2 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
